It should be like this:

PUSH-USERSPACE happens automatically:

- at VM toplevel entry calls (eval_string() etc)
- inside synchronous JS-LAMBDAs and asynchronous JS-CALLBACKs

PUSH-USERSPACE adds a CONTINUATION-BARRIER, so trying to escape it
always raises.
