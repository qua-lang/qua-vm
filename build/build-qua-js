#!/usr/bin/env node
var fs = require("fs");
var browserify = require("browserify");
var b = browserify("./src/main.js");
b.require("./build/out/test.json", { expose: "qua-user-code" });
b.bundle().pipe(fs.createWriteStream("build/out/qua.js"));
